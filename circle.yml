test:
  override:
     - npm -v
     -  git clone git@github.com:nightwatchjs/nightwatch.git
    - cd nightwatch
    - npm install
    - nightwatch -v
     - npm install nightwatch
     - nightwatch -e chrome,firefox